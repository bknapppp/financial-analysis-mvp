import{notFound}from"next/navigation";import{AppShell}from"@/components/layout/app-shell";import{FindingsPage}from"@/features/findings/findings-page";import{loadFindingsPage}from"@/features/findings/findings-loader";export const revalidate=60;export default async function Page({params}:{params:Promise<{companyId:string}>}){const{companyId}=await params,m=await loadFindingsPage(companyId);if(!m)notFound();return <AppShell model={m.shell}><FindingsPage model={m}/></AppShell>}
